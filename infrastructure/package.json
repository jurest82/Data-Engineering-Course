{
  "name": "data-eng-course-infrastructure",
  "devDependencies": {
    "@serverless/safeguards-plugin": "^1.2.1",
    "serverless-deployment-bucket": "^1.6.0",
    "prettier": "^3.9.6",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8"
  },
  "scripts": {
    "format": "prettier --write . && yapf -i -r src/ && python3 -m isort src/",
    "format:check": "prettier --check . && yapf --diff -r src/ && python3 -m isort --check-only src/"
  }
}
