{
  "name": "data-eng-course-backend",
  "devDependencies": {
    "@serverless/safeguards-plugin": "^1.2.1",
    "serverless-deployment-bucket": "^1.6.0",
    "prettier": "^3.9.6",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8"
  },
  "scripts": {
    "format": "prettier --write . && yapf -i -r src/ database/ && python3 -m isort src/ database/",
    "format:check": "prettier --check . && yapf --diff -r src/ database/ && python3 -m isort --check-only src/ database/"
  }
}
